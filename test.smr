func writeNum(argument)
    write(argument);
end


begin
    read(arg1, arg2);
    arg1 := 0 - arg1;

    if(arg1>arg2 & arg1>0)
        arg2 := arg2 - 1;
    else
        arg2 := 4;
    end

    num := readNum();
    write(arg1,(4*arg2)/2);
end

func readNum
    read(readnumarg);
    return readnumarg;
end

func factorial(n)
    if(n <= 1.0)
        a := 1.0;
        return a;
    else
        b := factorial(n-1) * n;
        return b;
    end
end