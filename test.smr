func writeNum(argument)
    write(argument);
    return 0;
end


begin
    read(arg1, arg2);
    arg1 := 0 - arg1;

    if(arg1>arg2 & arg1>0)
        arg2 := arg2 - 1;
    else
        arg2 := 4;
    end
    write(factorial(5));
    num := readNum();
    write(arg1,(4*arg2)/2);
    return 45;
end

func readNum
    read(readnumarg);
    return readnumarg;
end

func factorial(n)
    if(n <= 1.0)
        return 1.0;
    else
        return factorial(n-1) * n;
    end
end